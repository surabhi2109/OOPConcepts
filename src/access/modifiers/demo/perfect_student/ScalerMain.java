package access.modifiers.demo.perfect_student;

import access.modifiers.demo.extra_ordinary_student.Scaler;

public class ScalerMain {
    //Scaler obj = new Scaler();    -- default class cannot be accessed in different package
}
